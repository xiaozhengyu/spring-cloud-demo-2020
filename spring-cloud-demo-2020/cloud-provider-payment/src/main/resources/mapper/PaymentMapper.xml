<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.xzy.repository.PaymentMapper">
    
    <resultMap id = "baseResultMap" type = "com.xzy.entities.PaymentEntity">
        <id column = "id" property = "id" jdbcType = "BIGINT"/>
        <result column = "serial" property = "serial" jdbcType = "VARCHAR"/>
    </resultMap>
    
    <!--新增支付信息-->
    <insert id = "save" parameterType = "com.xzy.entities.PaymentEntity" useGeneratedKeys = "true" keyProperty = "id">
        insert into payment(serial)
        values (#{serial})
    </insert>
    
    <!--根据ID查找支付信息-->
    <select id = "findByPrimaryKey" parameterType = "long" resultMap = "baseResultMap">
        select id, serial
        from payment
        where id = #{id}
    </select>
</mapper>